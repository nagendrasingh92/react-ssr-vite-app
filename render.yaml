services:
  - type: web
    name: react-ssr-vite
    env: node
    plan: free
    buildCommand: |
      npm install
      npm run build
    startCommand: npm start
    nodeVersion: 18
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
